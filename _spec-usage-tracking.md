# สเปค — เก็บข้อมูลการใช้งานจอ (usage tracking)

**สถานะ (2026-08-12): ระบบทำงานครบวงจรบน production แล้ว** — ไฟล์นี้คือเอกสารหลักของงานนี้
session ที่รับช่วงต่อ: อ่านไฟล์นี้ + ดู commit ที่อ้างในตาราง แล้วทำงานต่อได้เลย

| ส่วน | สถานะ | อยู่ที่ |
|---|---|---|
| ขั้น 0 · `mediaId` ใน projection สองฝั่ง | ✅ `e7aae3e` | `vertical-signage.html` + `build.mjs` |
| ตัวนับ 12 hook ในจอ + เกาะ beacon | ✅ `e7aae3e` ทดสอบครบ | `vertical-signage.html` (ค้น `USAGE`) |
| §5 API รับ `u` · atomic increment · นับสแกน + กันบอท | ✅ `daaf03d` | repo `aquamx-handoff`: `lib/usage.ts` · `kiosk-beacon` · `/m/o` `/m/p` |
| GET `/api/kiosk-usage` + ชื่อสื่อแนบมา | ✅ `4942995` | `app/api/kiosk-usage/route.ts` |
| §6 mirror รายคืนเข้า Sanity (`usageDaily`) | ✅ `638e7c5` + backfill แล้ว | `netlify/functions/usage-daily-sync.mjs` (22:30 ไทย) |
| §7 หน้า Studio "การใช้งานจอ" + fallback อ่าน mirror | ✅ `22fcbf3` deploy แล้ว | `sanity-studio/tools/UsageTool.tsx` + `usageMath.ts` |
| §8 บล็อกสรุปรายสัปดาห์ใน LINE รายงานเช้า | ⬜ ยังไม่ทำ | ต่อท้าย `/api/kiosk-report` เฉพาะเช้าวันจันทร์ |
| นับ abandon บนหน้ามือถือ | ⬜ รอข้อมูลจริงก่อน | — |
| รายงานผู้ลงโฆษณา | ⬜ ทำหลังเก็บครบ 1 เดือน | ใช้เอกสาร `usageDaily` เป็นแหล่ง |

### บทเรียนที่จ่ายจริงแล้ว — คนรับช่วงต้องรู้

1. **route ใหม่ใน aquamx-handoff ต้องลงทะเบียน `OPEN_PATHS` ใน `middleware.ts`** ไม่งั้น 401
   (kiosk-usage เคยโดนมา 3 วันโดยไม่มีใครรู้ — guard เพิ่มทีหลัง route)
2. **Studio เปิดได้ทาง sanity.io dashboard (iframe) เท่านั้นแล้ว** — โดเมนตรง 302 ไปแล้ว
   ใน iframe เบราว์เซอร์บล็อก fetch ข้ามโดเมนทั้งหมด → หน้า Studio ใด ๆ ที่ยิง `app.aquamx.biz`
   จะพังในโหมดนั้น ทางแก้เดียวคือให้ข้อมูล mirror อยู่ใน Sanity (แบบ `usageDaily`)
   — **`HealthTool` / `KioskHealthTool` ยังมีปัญหานี้อยู่ ยังไม่ได้แก้**
3. **จอส่งยอดเป็นส่วนต่าง ล้างเมื่อเซิร์ฟเวอร์ตอบ `usage:"ok"` ในบอดี้เท่านั้น** — ห้ามเปลี่ยน
   สัญญานี้ฝ่ายเดียว (ดูคอมเมนต์ใน beacon route + ตัว player)
4. **ป้ายปุ่มเมนูเปลี่ยน MENU → OFFERS เมื่อ 2026-08-11 (`258b20a`)** — ตัวนับ `menu` คือ
   อัตรากดปุ่มนี้ กำลังใช้วัดผลการเปลี่ยนคำ อย่าเพิ่งแตะป้ายนี้ซ้ำจนกว่าจะมีข้อมูลพอเทียบ

### ตัวเลขจริง ณ วันส่งมอบ (3 วันแรก)

ออกอากาศรวม ~130k · แตะสไลด์ 38 · กดปุ่มเมนู 146 (คนใช้ปุ่มมากกว่าแตะสไลด์ ~4 เท่า) ·
ผู้ใช้ 134 · สแกน 28 · ชั่วโมงคึกสุด 18:00 — อย่าสรุปอะไรจนครบ 2–4 สัปดาห์

### แก้จากสเปคเดิม 2 จุด (ตัดสินตอนลงมือ)

1. **ความล้มเหลวของตัวนับต้องไม่ทำให้ beacon พัง** — เดิมเขียนว่าถ้าเขียนตัวนับไม่สำเร็จ
   ให้ตอบ 500 แต่ endpoint นี้คือแหล่งเดียวที่บอกว่าจอไหน online — ถ้าตัวนับพัง
   ทั้งกองจะกลายเป็น "จอตาย" ทันที เปลี่ยนเป็น **ตอบ 200 พร้อม `usage: 'failed'` ในบอดี้**
   จอจะเก็บยอดไว้ยิงใหม่รอบหน้า ได้ทั้งสองอย่างโดยไม่แลกกัน
2. **QR ของ provider ไม่เคยบอกว่ามาจากจอไหน** — `/m/p/<slug>` ไม่มี `?p=` เลย
   (ของ offer มีอยู่แล้ว) เติมให้ ไม่งั้นการสแกนจาก 6 จอจะกองรวมเป็น "unknown" ก้อนเดียว

### หน้า Studio — สิ่งที่ไม่ได้ทำ และเพราะอะไร

- **ไม่มีคอลัมน์ lead** — สคีมา `lead` ไม่มีฟิลด์โปรเจกต์/จอเลย (มีแต่ในข้อความ `notes`)
  จะดึงมาต่อจอได้ต้องเดาจากข้อความ ซึ่งบนแดชบอร์ดคือการโกหกแบบดูน่าเชื่อ —
  ถ้าอยากได้จริงต้องเพิ่มฟิลด์ `project` ใน `lead` ก่อน (คนละงาน)
- **แยก “จอไม่ได้เปิด” จาก “ไม่มีคนแตะ” ด้วยจำนวนออกอากาศ** ไม่ใช่ `upMin` — จอที่ยัง
  ออกอากาศอยู่ย่อมส่งเลขออกอากาศมาเสมอ ถ้าเป็น 0 แปลว่าไม่ได้ทำงานจริง ตรงกว่าและ
  ไม่ต้องเก็บฟิลด์เพิ่ม
- **ส่วนคำนวณแยกไฟล์** (`usageMath.ts`) เพื่อให้กฎที่มี “ความหมาย” (เรียงตามอัตรา ·
  สื่อที่ไม่เคยถูกแตะ · จอที่ปิด) ทดสอบได้โดยไม่ต้องเปิด Studio

### ที่เก็บจริงเป็น **field แบน ไม่ใช่ map ซ้อน**

client Firestore ในโปรเจกต์นี้พูด REST field format และรับเฉพาะค่าสเกลาร์ — และที่สำคัญกว่านั้น
`increment` ทำงานกับ field path แบน ๆ ซึ่งเป็นสิ่งที่ทำให้ **สองจอของโปรเจกต์เดียวกัน
(39-by-sansiri มี 2 จอ) เขียนพร้อมกันได้โดยยอดไม่หาย** — read-modify-write จะทับกันเอง

```
air · tap · sess · detail · gallery · cta · qr · phone · menu · cat
end_back · end_cta · end_idle · dw_d0 · dw_d3 · dw_d10 · dw_d30
h14_air · h14_tap · h14_sess · h14_detail · h14_cta · h14_qr · h14_phone
m_<mediaId>_air · m_<mediaId>_tap
```

ทุกตัวเลขในสเปคนี้วัดจากระบบจริง ไม่ได้ประมาณ:
โปรเจกต์ `39-by-sansiri` มี **19 สไลด์ · ลูปละ 5.1 นาที** · เปิดจอ **6:00–22:00** →
ออกอากาศ **~11.8 รอบ/ชม.** = **~189 ครั้ง/สไลด์/วัน** ≈ 3,600 การออกอากาศ/จอ/วัน ·
ป๊อปอัปกลับหน้าหลักเองที่ 30 วิ + นับถอยหลังอีก 30 วิ (`resetIdle`, บรรทัด 4903)

---

## 1. คำถามที่ระบบนี้ต้องตอบให้ได้

1. สื่อชิ้นไหนถูกแตะ ชิ้นไหน**ไม่เคยถูกแตะเลย** (ต่อการออกอากาศ ไม่ใช่ยอดดิบ)
2. ช่วงเวลาไหนของวันคนใช้จอ
3. คนหลุดออกจากเส้นทางตรงขั้นไหน
4. จอไหนมีคนใช้จริง จอไหนเงียบ
5. โฆษณาของผู้ลงโฆษณารายนี้ถูกแตะกี่ครั้งในเดือนที่ผ่านมา ← ตัวที่เอาไปออกรายงาน

**สิ่งที่ระบบนี้ตอบไม่ได้และห้ามอ้าง:** จำนวนผู้ชม / คนเดินผ่าน / impressions
เราไม่มีกล้องและไม่มีเซ็นเซอร์ ตัวส่วนที่แท้จริงคือ **"จำนวนครั้งที่ออกอากาศ"** เท่านั้น
เวลาเสนอลูกค้าให้พูดว่า "ถูกแตะ X ครั้ง จากการออกอากาศ Y ครั้ง" ห้ามแปลงเป็นจำนวนคน

---

## 2. หลักการที่ห้ามละเมิด (5 ข้อ)

| # | กฎ | เหตุผล (มีหลักฐาน) |
|---|---|---|
| 1 | **ไม่ยิงเน็ตตอนคนกำลังแตะ** — นับสะสมในตัวแปร ส่งเกาะไปกับ beacon ทุก 5 นาที | กล่อง ZC-H358S ANR ง่าย · beacon ยิงอยู่แล้วเพื่อเช็คสุขภาพจอ เราเติมแค่ ~300 ไบต์ในคำขอที่เกิดขึ้นอยู่แล้ว |
| 2 | **ห้ามพึ่ง localStorage เป็นที่เก็บยอดสะสม** | beacon จริงมี **46 แถวสำหรับจอจริง ~6 ตัว** (mahogany-tower ตัวเดียว 10 แถว) เพราะ `bid` อยู่ใน localStorage แล้วโดนล้างเป็นประจำ ยอดสะสมจะหายแบบเดียวกัน |
| 3 | **นับเฉพาะตอนหน้าถูกมองเห็นจริง** (`document.visibilityState === 'visible'`) | บางกล่องถูกห่อด้วย Yodeck ที่อาจสลับหน้าเราออกแต่ยังโหลดค้าง → ลูปเดินต่อทั้งที่ไม่มีใครเห็น ยอดออกอากาศจะเฟ้อ ตัวส่วนผิด |
| 4 | **ตัดโหมดพรีวิวออกทั้งหมด** — `PREVIEW_MODE` (บรรทัด 5026) จริง = ไม่นับ ไม่ส่ง | ใน 46 แถวนั้นเป็น HeadlessChrome / Electron / Edge เกินครึ่ง = เบราว์เซอร์ทีมงาน + iframe พรีวิวใน Studio ถ้าไม่กัน ตัวเลขจะเด้งทุกครั้งที่ทีมเปิดดูสื่อ |
| 5 | **รวมข้อมูลตามโปรเจกต์ ไม่ใช่ตามกล่อง** และ **เซิร์ฟเวอร์เป็นคนประทับเวลา** | กล่องเปลี่ยน `bid` เองได้ (ข้อ 2) · นาฬิกากล่องเชื่อไม่ได้ · ยิงทุก 5 นาทีอยู่แล้ว ความคลาดเคลื่อนไม่เกินนั้น |

ผลจากกฎข้อ 1–2: กล่องถือแค่ **"ยอดที่ยังไม่ได้ส่ง"** ล้างค่าเมื่อได้ HTTP 200 เท่านั้น
ยิงพลาดก็สะสมต่อ ไม่หาย · ยอดสะสมระยะยาวเป็นหน้าที่ของเซิร์ฟเวอร์ล้วน

---

## 3. สิ่งที่นับ

### 3.1 ต่อสไลด์ (key = `mediaId`)

`mediaId` = **`_id` ของเอกสาร media ใน Sanity** — ห้ามใช้ index ของเพลย์ลิสต์ (ลำดับเปลี่ยน
ทุกสัปดาห์) และไม่ใช้ slug ของ offer ด้วย เหตุผล:

- **slug แก้ได้ แต่ `_id` แก้ไม่ได้** — วันที่มีคนแก้ slug ประวัติจะขาดสองท่อนเงียบ ๆ
- **สื่อหลายชนิดไม่มี offer เลย** (ประกาศนิติฯ · บอร์ดราคาห้อง) → slug เป็น null
  ต้องตกไปใช้ `_key` ซึ่งไม่เสถียร ส่วน media id ครอบคลุมทุกชนิดเท่ากัน
- **เทียบรูปต่อรูปได้** — ร้านเปลี่ยนรูปใหม่แล้วรู้ว่ารูปไหนถูกแตะมากกว่า ระดับ offer จะกลืนรวม

**ข้อแลก:** ร้านอัปโหลดรูปใหม่ = media id ใหม่ = กราฟระดับ media ขาดเป็นสองท่อน
แก้โดย **ตอนสรุปรายวันผูกกลับไปที่ offer ด้วย** (ข้อ 6) → ได้ทั้งความต่อเนื่องระดับแคมเปญ
สำหรับรายงานลูกค้า และการเทียบรูปต่อรูปสำหรับตัดสินใจภายใน

> **ขั้นที่ 0 ของงานนี้:** ตอนนี้ `media->_id` **เดินทางมาไม่ถึงจอ** — projection ส่งมาแค่
> `offerSlug` ต้องเพิ่มฟิลด์ **ทั้งสองที่ ไม่งั้นจอที่ดึงสดกับจอที่ bake จะเพี้ยนกัน**:
> `vertical-signage.html` (live projection, ~บรรทัด 2443) และ `build.mjs` (`PLAYLIST_PROJ_V7`, ~บรรทัด 197)

| ตัวนับ | นับเมื่อ | จุดในโค้ด |
|---|---|---|
| `air` | สไลด์ขึ้นจอครบ 1 ครั้ง (และหน้ามองเห็นอยู่) | `showSlot()` บรรทัด 3389 |
| `tap` | แตะสไลด์นั้นจนเปิดป๊อปอัป | ตัวจับ `closest('.cta, .slot-more')` บรรทัด 4674 |

`tap/air` = อัตราการถูกแตะ ตัวเลขหลักที่ใช้ตัดสินใจ

### 3.2 กรวย 7 ขั้น (นับรวมทั้งจอ ไม่แยกสไลด์)

| ขั้น | ตัวนับ | จุดในโค้ด |
|---|---|---|
| 1 ออกอากาศ | `air` (รวมจาก 3.1) | — |
| 2 แตะสไลด์ | `tap` (รวมจาก 3.1) | — |
| 3 เปิดป๊อปอัป | `detail` | `openDetail()` 4339 |
| 4 เลื่อนดูรูปในป๊อปอัป | `gallery` | ตัวจับแตะรูป 4075 |
| 5 กด CTA | `cta` | ตัวจับ `.cta` 4674 (แยกจาก tap ด้วย `ctaType`) |
| 6 เห็นหน้า QR | `qr` | `showHandoff()` 4504 |
| 7 กดส่งเบอร์บนจอ | `phone` | `sendLinkBtn` 4668 |

**ขั้นที่ 8 (สแกนจริง) นับที่เซิร์ฟเวอร์** — ดูข้อ 5

เพิ่มอีกสองตัวที่ไม่อยู่ในกรวยแต่มีค่า:
- `menu` — เปิดเมนู/หมวด (`isMenuOpen = true` บรรทัด 3764 และ 3909)
- `cat` — เข้าหน้าหมวดเต็มจอ (`catScreen.classList.add('open')` 3908)

### 3.3 คุณภาพของการเปิดป๊อปอัป

**สาเหตุที่ปิด** — นับแยก 3 ทาง (`closeDetail()` 4466 · `returnToMain()` 4939)

| ตัวนับ | ความหมาย |
|---|---|
| `endBack` | กดย้อนกลับ/ปิดเอง = อ่านจบแล้วออก |
| `endCta` | กดต่อไปขั้นถัดไป = สนใจจริง |
| `endIdle` | หมดเวลา 60 วิ = เปิดค้างแล้วเดินหนี |

**เวลาที่อยู่ในป๊อปอัป** — เก็บเป็น**ช่วง 4 ถัง** ไม่เก็บค่าดิบ:
`d0` = 0–3 วิ (แตะโดนบังเอิญ) · `d3` = 3–10 · `d10` = 10–30 (สนใจจริง) · `d30` = 30+ วิ

ถ้าไม่แยกสองอย่างนี้ ยอด "แตะ" จะดูดีเกินจริง เพราะแตะโดนบังเอิญกับสนใจจริงถูกนับเท่ากัน

### 3.4 กันการนับเฟ้อ — session

แตะที่ห่างกัน **ไม่เกิน 60 วินาที นับเป็นคนเดียวกัน** (`sess` +1 เมื่อแตะครั้งแรกหลังเงียบ ≥60 วิ)
เด็กหรือแม่บ้านแตะรัว 8 ครั้งต้องไม่กลายเป็นผู้สนใจ 8 คน — รายงานให้ผู้ลงโฆษณาใช้ `sess`
ส่วน `tap` ใช้ดูภายใน

---

## 4. ฝั่งจอ — `vertical-signage.html`

### 4.1 โครงตัวนับ

```
U = {
  air:  { <mediaId>: n },      // ออกอากาศ
  tap:  { <mediaId>: n },      // ถูกแตะ
  f:    { detail, gallery, cta, qr, phone, menu, cat, sess },
  end:  { back, cta, idle },
  dw:   { d0, d3, d10, d30 },
}
```

ตัวเลขล้วน ไม่มีข้อความ ไม่มีเวลา ไม่มีอะไรที่ระบุตัวบุคคล

**ขนาด:** `_id` ของ Sanity ยาว ~36 ตัวอักษร · 19 สไลด์ × 2 ตัวนับ ≈ **1.4 KB ต่อการส่ง**
= ~270 KB/วัน/จอ ไม่มีนัยสำคัญ **ส่ง id เต็ม ห้ามตัดให้สั้น** — ตัดเหลือ 8 ตัวแรกก็แทบไม่ชนกัน
จริง แต่จะทำให้สอบทานย้อนหลังเทียบกับ Sanity ตรง ๆ ไม่ได้ ไม่คุ้มกับที่ประหยัด

### 4.2 เกาะไปกับ beacon เดิม (บรรทัด 5167)

เติมฟิลด์ `u` เข้าไปใน body เดิม ไม่สร้าง timer ใหม่ ไม่สร้าง endpoint ใหม่:

```
{ p, rev, bid, up, err, imgf, slide, memMB, scr, board, andr, ua,
  u: { air:{…}, tap:{…}, f:{…}, end:{…}, dw:{…} } }
```

- ถ้า `PREVIEW_MODE` เป็นจริง → **ไม่แนบ `u` เลย** (กฎข้อ 4)
- ถ้าไม่มีอะไรเปลี่ยนเลยตั้งแต่ส่งครั้งก่อน → ไม่แนบ `u` (ลดขนาด)
- ล้าง `U` **หลังได้ 200 เท่านั้น** — ยิงพลาดให้สะสมทับไปเรื่อย ๆ
- ยอดที่ยังไม่ได้ส่งตอนไฟดับ 22:00 จะเสียไม่เกิน 5 นาที (ถ้าส่งรายชั่วโมงจะเสียถึง 59 นาที
  ของชั่วโมง 21–22 น. ทุกวัน ซึ่งน่าจะเป็นชั่วโมงที่คนกลับเข้าคอนโดมากที่สุด)

---

## 5. ฝั่ง API — `aquamx-handoff`

### 5.1 รับยอดจากจอ

`POST /api/kiosk-beacon` (route เดิม) — ถ้ามีฟิลด์ `u` ให้บวกเข้าเอกสารรายวัน

**Firestore** `config` collection (ที่เดียวกับ beacon เดิม — rules เปิดไว้แล้ว):

```
kioskUsage-<project>-<YYYY-MM-DD>
{
  project, date,
  hours: {                       // 16 ถัง 06–21 (ชั่วโมงประทับโดยเซิร์ฟเวอร์ Asia/Bangkok)
    "06": { air, tap, sess, detail, cta, qr, phone },
    …
    "21": { … }
  },
  media: { "<mediaId>": { air, tap } },   // คีย์ด้วย _id ของ media — ดิบ ไม่ผูก offer ตรงนี้
  funnel: { detail, gallery, cta, qr, phone, menu, cat, sess },
  end:    { back, cta, idle },
  dwell:  { d0, d3, d10, d30 },
  upMin:  <นาทีที่จอเปิดจริงวันนั้น>,     // จาก beacon — แยก "จอปิด" ออกจาก "ไม่มีคนแตะ"
  updatedAt
}
```

1 เอกสาร/โปรเจกต์/วัน — 6 จอ × 365 วัน ≈ 2,200 เอกสาร/ปี เล็กมาก
**ห้ามคีย์ด้วย `bid`** (กฎข้อ 5) ไม่งั้นข้อมูลจะแตกเป็นผีเหมือน beacon

### 5.2 นับการสแกนจริง

หน้า `/m/o/[offerSlug]` และ `/m/p/[providerSlug]` — บวก 1 ตอนหน้าโหลดฝั่งเซิร์ฟเวอร์
ไม่ใช้คุกกี้ ไม่เก็บ IP ไม่เก็บ UA อ่านโปรเจกต์จาก `?p=` ที่มีอยู่ในลิงก์ QR แล้ว

```
kioskScan-<project>-<YYYY-MM-DD>
{ project, date, offers: { "<slug>": n }, total, updatedAt }
```

**ช่องว่างระหว่าง `qr` (จอโชว์ QR) กับ `scan` คือข้อมูลที่มีค่าที่สุดในระบบนี้** —
โชว์ QR 40 ครั้งแต่สแกนจริง 3 = ปัญหาอยู่ที่ QR (เล็กไป สูงไป แสงสะท้อน คนไม่กล้าสแกน)
ไม่ใช่ที่คอนเทนต์ ถ้าไม่แยกนับจะโทษคอนเทนต์ผิดตัว

---

## 6. สรุปรายวันเข้า Sanity

**ข้อมูลดิบไม่เข้า Sanity** — เพราะ dataset `production` ผูกกับ webhook → rebuild chain ของทุกคอนโด
และเอกสาร Sanity เก็บประวัติทุกเวอร์ชัน ตัวนับที่ขยับ ~190 ครั้ง/วัน/จอ จะทำให้ history บวมเปล่า ๆ
รวมทั้งจะไปโผล่ปนในเครื่องมือ "รอปล่อยขึ้นจอ" กับงานคอนเทนต์จริง

**แต่สรุปรายวันเข้า** — วันละครั้ง (ต่อท้าย netlify function รายงานเช้า 08:00 ที่มีอยู่แล้ว):

```
_type: 'usageDaily'
{ project → ref, date, air, tap, sess, scan, lead,
  hours: [16 ตัวเลข],
  // ผูกกลับสองระดับตอนสรุป — จอไม่ต้องรู้เรื่องนี้
  byMedia: [ { media → ref, air, tap } ],            // เทียบรูปต่อรูป (ภายใน)
  byOffer: [ { offer → ref, air, tap, sess } ] }     // ต่อเนื่องระดับแคมเปญ (รายงานลูกค้า)
```

การผูก `mediaId → offer` ทำตอนสรุปด้วย GROQ ครั้งเดียว (`*[_id in $ids]{_id, "offer": offer->_id}`)
สื่อที่ไม่มี offer (ประกาศ/บอร์ด) จะอยู่ใน `byMedia` อย่างเดียว ไม่โผล่ใน `byOffer` — ถูกต้องแล้ว

**ต้องเช็คก่อนลงมือ:** filter ของ Sanity webhook ต้องไม่รวม `_type == "usageDaily"`
ไม่งั้นจะกระตุ้น rebuild ทุกคอนโดวันละครั้งโดยไม่จำเป็น — เช็คที่ hook list ใน Sanity
ไม่ใช่แค่ในโค้ด `sanity-webhook/route.ts` (เคยพลาดเพราะดูแค่โค้ดมาแล้ว)

เอกสารนี้มีไว้เพื่อ 2 อย่าง: Studio โชว์ตัวเลขข้าง ๆ ที่ทีมทำงานอยู่จริง
และออกรายงานให้ผู้ลงโฆษณาได้โดยไม่ต้องต่อ Firestore

---

## 7. หน้าแสดงผลใน Studio

**ไม่สร้างแอปใหม่** — เพิ่มเครื่องมือเดียวใน `sanity-studio/tools/` ข้าง ๆ
`DashboardTool` / `KioskHealthTool` / `ContentFootprintTool` ที่มีอยู่แล้ว
ทีมเปิด Studio ทุกวันเพื่อกด publish อยู่แล้ว ไม่ต้องจำ URL ใหม่

**ชื่อ:** `UsageTool.tsx` — "การใช้งานจอ"

หน้าเดียว ตอบ 3 คำถาม เรียงจากบนลงล่าง:

```
┌─ ช่วงเวลา: [7 วัน] [30 วัน]        โปรเจกต์: [ทั้งหมด ▾] ──┐
│                                                             │
│  A. จอไหนมีคนใช้                                            │
│     แถวละจอ: แตะ · session · สแกน · lead · ชั่วโมงเปิดจริง  │
│     จอที่ปิด/ตาย = สีเทา + ป้าย "จอไม่ได้เปิด" ห้ามโชว์ 0 เฉย ๆ │
│                                                             │
│  B. สื่อไหนถูกแตะ                                            │
│     เรียงตาม tap/air จากมากไปน้อย · โชว์ทั้งอัตราและยอดดิบ    │
│     สื่อที่ air > 200 แต่ tap = 0 → ป้ายแดง "ยังไม่เคยถูกแตะ"  │
│                                                             │
│  C. คนหลุดตรงไหน                                             │
│     กรวย 8 ขั้นเป็นแท่งลดหลั่น + % ที่หลุดแต่ละขั้น           │
│     ใต้กรวย: แถบชั่วโมง 6–22 (16 ช่อง) เข้มอ่อนตามยอดแตะ      │
└─────────────────────────────────────────────────────────────┘
```

**กฎการออกแบบ (สำคัญเพราะ n จะน้อยมากช่วงแรก — ทั้งระบบมี 7 leads)**

- **ห้ามใช้กราฟเส้น** — ขึ้นจาก 3 เป็น 5 จะดูเหมือนเทรนด์ทั้งที่เป็นความบังเอิญ
- ใช้**ยอดรวมของช่วง + การจัดอันดับ** ไม่ใช่การเปลี่ยนแปลงรายวัน
- โชว์ **อัตราต่อการออกอากาศ** ควบคู่ยอดดิบเสมอ ไม่งั้นสื่อที่ลงนานกว่าชนะตลอดโดยไม่แฟร์
- แยก **"จอปิด"** ออกจาก **"ไม่มีคนแตะ"** ด้วยสีและคำ ห้ามให้ปนกัน
- ถ้าข้อมูลน้อยกว่า 7 วัน ให้ขึ้นคำเตือน "ข้อมูลยังน้อยเกินกว่าจะสรุป"

---

## 8. รายงาน LINE

`/api/kiosk-report` ส่งสุขภาพจอเข้ากลุ่มทุกเช้า 08:00 อยู่แล้ว —
เติม**บล็อกสรุปการใช้งานสัปดาห์ละครั้ง (เช้าวันจันทร์เท่านั้น)** ต่อท้ายรายงานเดิม
ไม่เพิ่มข้อความใหม่ ไม่กินโควตาเพิ่ม (แผนฟรี 300 ข้อความ/เดือน แชร์กับ lead อยู่แล้ว)

```
📊 การใช้งาน 7 วัน
39 by Sansiri   แตะ 34 · สแกน 6 · lead 2
mahogany-tower  แตะ 12 · สแกน 1 · lead 0
สื่อที่ยังไม่เคยถูกแตะ: 4 ชิ้น
```

---

## 9. ห้ามทำ

1. **ห้ามยิงต่อการแตะ 1 ครั้ง** ไม่ว่าจะดูเบาแค่ไหน — กล่อง ZC ANR ง่าย
2. **ห้ามเก็บอะไรที่ระบุตัวบุคคล** ไม่มี IP ไม่มี UA ไม่มีคุกกี้ ไม่มีเวลาแบบละเอียด — ตัวนับล้วน
3. **ห้ามใช้ index ของเพลย์ลิสต์เป็น key** ลำดับเปลี่ยนทุกสัปดาห์
4. **ห้ามนับตอน `PREVIEW_MODE`** หรือตอนหน้าไม่ได้ถูกมองเห็น
5. **ห้ามแตะกิ่ง booking / lead / handoff** ที่ทำงานอยู่แล้ว งานนี้เพิ่มตัวนับอย่างเดียว
6. **ห้ามเรียกตัวเลขนี้ว่า "ผู้ชม" หรือ "impressions"** ในทุกหน้าจอและทุกรายงาน

---

## 10. วิธีทดสอบ

1. เปิด player ในเบราว์เซอร์ธรรมดา แตะสไลด์ 3 ครั้ง เปิดป๊อปอัป กด CTA กดดู QR
   → ดัก `fetch` ตรวจว่า `u` ที่จะส่งมีตัวเลขถูกต้องครบทุกช่อง
2. เปิดด้วย `?preview=1` ทำแบบเดียวกัน → **ต้องไม่มี `u` ในเพย์โหลดเลย**
3. ซ่อนแท็บ (สลับแท็บ) ทิ้งไว้ 2 นาที → `air` ต้องไม่เพิ่ม
4. ตัดเน็ตแล้วแตะ 5 ครั้ง ต่อเน็ตกลับ → ยอดต้องครบ 5 ไม่หาย
5. แตะรัว 8 ครั้งใน 10 วินาที → `sess` ต้องเพิ่ม 1 ไม่ใช่ 8
6. ยิง `u` ปลอมเข้า API 2 ครั้ง → เอกสารรายวันต้องบวกสะสม ไม่ใช่ทับ
7. เปิดหน้า `/m/o/...` 3 ครั้ง → `kioskScan-*` ต้องเป็น 3
8. ตรวจว่า `usageDaily` ที่เขียนเข้า Sanity **ไม่กระตุ้น rebuild** (ดู deploy log ของคอนโดสักตัว)
9. เทียบกับความจริง: เดินไปแตะจอจริงที่ไซต์ 5 ครั้ง แล้วดูว่าตัวเลขขึ้นครบ

---

## 11. ขอบเขต

**เฟส 1 (สเปคนี้)** — sync `mediaId` เข้า projection ทั้งสองฝั่ง · ตัวนับในจอ · เก็บลง Firestore · นับสแกน · สรุปรายวันเข้า Sanity ·
หน้า Studio · บล็อกใน LINE รายสัปดาห์

**ยังไม่ทำ**
- นับ abandon ในหน้ามือถือ (เลือกห้องแล้วไม่กดส่ง) — รอให้มีข้อมูลจริงก่อน
- heatmap แบบพิกัด — จอ 9:16 ปุ่มใหญ่ มีไม่กี่จุด รู้แล้วก็ทำอะไรต่อไม่ได้
- รายงานรายผู้ลงโฆษณาแบบส่งอัตโนมัติ — ทำหลังเก็บครบ 1 เดือน

**อย่าเพิ่งตัดสินใจอะไรจากข้อมูล 1 สัปดาห์** ทั้งระบบมี 7 leads ควรเก็บ 2–4 สัปดาห์ก่อนสรุป
