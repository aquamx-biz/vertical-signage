# Handoff — ป๊อปอัป: จำกัดพาดหัว + คำอธิบายเหลือ 1 บรรทัด แล้วคืนพื้นที่ให้รายการ CTA

เขียนไว้ให้ session ที่ถือ `vertical-signage.html` ทำต่อ · วัดบน commit `6155143` · 2026-08-05

---

## 1. โจทย์

`.detail-dyn` (กล่องเนื้อหาต่อ CTA — รายการสั่งซื้อ, ปฏิทินจอง, ป้ายจุดเด่นร้าน) **ได้พื้นที่ไม่เท่ากันในแต่ละใบ
เพราะมันคือเศษที่เหลือจากพาดหัวและคำอธิบาย** ร้านที่เขียนคำอธิบายยาวจึงถูกลงโทษด้วยการที่เมนู/ปฏิทิน
ของตัวเองหดลงเกือบครึ่ง

ข้อเสนอจากเจ้าของงาน: จำกัดพาดหัวและคำอธิบายให้เหลืออย่างละ 1 บรรทัด เกินแล้วต่อท้ายด้วย `...more`
(แบบเดียวกับสไลด์) เพื่อดันข้อความขึ้นไปด้านบนและคืนพื้นที่ให้รายการด้านล่าง

---

## 2. ตัวเลขที่วัดได้จริง (9:16 · 540×960 · การ์ด 523px · panel 53% = 276px)

**พื้นที่ที่ `.detail-dyn` ได้อยู่ตอนนี้**

| กรณี | พื้นที่ |
|---|---|
| คำอธิบายสั้น 1 บรรทัด (เส้นทาง "แตะการ์ดในหน้าหมวดหมู่") | **192px** |
| คำอธิบายยาว 172 ตัวอักษร = 4 บรรทัด (เส้นทาง "แตะ CTA บนสไลด์") | **108px** |
| พาดหัว 2 บรรทัด | −22px จากทั้งสองค่าข้างบน |

**ต้นทุนของแต่ละส่วน** — คำอธิบาย 4 บรรทัด → 1 บรรทัด คืนมา **61px** · ตัด `margin-bottom: 2vh`
ใต้คำอธิบาย คืนอีก **19px** · พาดหัว 2 บรรทัด → 1 บรรทัด คืน **22px**

**ผลลัพธ์: panel กลายเป็น ~192px คงที่ทุกใบ ไม่ว่าร้านจะพิมพ์ยาวแค่ไหน**

**เทียบกับเนื้อหาจริง 27 โฆษณาบน 39-by-sansiri** (เปิดทีละใบแล้ววัดความสูงเนื้อหาจริง)

| พื้นที่ที่ให้ | จำนวนใบที่แสดงเนื้อหา CTA ครบโดยไม่ต้องเลื่อน |
|---|---|
| 108px (วันนี้ เมื่อคำอธิบายยาว) | 16 / 27 |
| **192px (หลังแก้)** | **20 / 27** |
| 247px (ถ้าย้ายข้อความออกจาก panel ทั้งหมด) | 21 / 27 |

**อ่านตารางนี้ให้ถูก** — กำไรหลักไม่ใช่ตัวเลข 16→20 แต่คือ **พื้นที่รายการหยุดผูกกับความยาวข้อความ
ที่ร้านพิมพ์มา** ซึ่งเป็นเหตุผลเดียวกับที่ panel ถูกล็อกไว้ที่ 53% อยู่แล้ว · และบรรทัดสุดท้ายบอกว่า
**ไม่ต้องทำมากกว่านี้** ดันต่ออีก 55px ได้เพิ่มแค่ใบเดียว ไม่คุ้มกับการรื้อดีไซน์

---

## 3. สิ่งที่ต้องแก้

**CSS**

- `vertical-signage.html:1644` — `.detail-cap .detail-title` เปลี่ยน `-webkit-line-clamp: 2` → `1`
  (หมายเหตุ: กฎฐานที่บรรทัด 1625 ตั้ง clamp 3 ไว้ แต่ของจริงที่มีผลคือกฎ `.detail-cap` ตัวนี้)
- `vertical-signage.html:1645` — `.detail-sub` เพิ่ม clamp 1 บรรทัด
  (`display:-webkit-box; -webkit-box-orient:vertical; -webkit-line-clamp:1; overflow:hidden`)
  และลด `margin-bottom` จาก `2vh` เหลือ `0.6vh`

  ระยะ margin ที่เหลือไว้ 0–0.8vh ไม่มีผลกับจำนวนใบที่พอดี — ความสูงเนื้อหาจริงของทั้ง 27 ใบ
  กระโดดจาก 146px ไป 199px ไม่มีใบไหนอยู่ในช่วง 186–192 ฉะนั้นเลือกตามความสวยได้เลย

**JS**

- ใช้ของที่มีอยู่แล้วซ้ำ: `MORE_LABEL` (บรรทัด 3126) และแพตเทิร์นใน `layoutSlotText()` (บรรทัด 3107)
  ซึ่งวัด `scrollWidth > clientWidth` แล้วค่อยต่อ chip — ฝั่งป๊อปอัปต้องวัด **ความสูง**
  (`scrollHeight > clientHeight`) แทนเพราะ clamp เป็นแนวตั้ง
- จุดที่ข้อความถูกเซ็ต: `openDetail()` บรรทัด 4288–4289 (`setTxt('detailTitle'…)` / `setTxt('detailSub'…)`)
  — ต่อ chip หลังจากสองบรรทัดนี้ทำงานแล้ว
- มาร์กอัป: `.detail-cap` + `#detailTitle` บรรทัด 1921–1923 · `#detailSub` บรรทัด 1926

---

## 4. กฎการโต้ตอบ (สำคัญ อย่าลอกจากสไลด์ตรง ๆ)

บนสไลด์ `...more` มีที่ให้ไปต่อคือป๊อปอัป **แต่ในป๊อปอัปมันคือปลายทางสุดท้ายแล้ว**
ฉะนั้น `...more` ในป๊อปอัปต้อง **กางข้อความออกตรงนั้นเมื่อแตะ แตะซ้ำหุบกลับ** ห้ามเปิดหน้าใหม่
ไม่งั้นคนที่อยากอ่านคำอธิบายเต็มจะไปไม่ถึง

ตอนกางออก ให้ `.detail-dyn` หดตามปกติ (flex อยู่แล้ว) — ไม่ต้องทำอะไรเพิ่ม

---

## 5. เคสที่ต้องยกเว้น — ป๊อปอัปประกาศ

`openDetail()` บรรทัด 4379: เมื่อ `ctx.isUpdate === true` โค้ดยัด **เนื้อความทั้งหมดของประกาศ**
ลงใน `#detailSub` (และซ่อน CTA/dyn ทิ้ง) ถ้า clamp เหลือ 1 บรรทัดโดยไม่ยกเว้น
**ประกาศจะเหลือข้อความบรรทัดเดียว** และไม่มีอะไรได้คืนเลยเพราะ dyn ถูกซ่อนอยู่แล้ว

→ ข้าม clamp เมื่อ `ctx.isUpdate` (เช่นใส่คลาส `update-mode` บน `.detail-card` แล้วปลด clamp ในคลาสนั้น)

---

## 6. วิธีวัดซ้ำ (วางใน console ของ player ที่ 9:16)

⚠️ **`dyn.scrollHeight` โกหก** เมื่อเนื้อหาสั้นกว่ากล่อง — มันคืนความสูงกล่องมาแทน ทำให้ 19 ใบ
อ่านได้ 192px เท่ากันหมดทั้งที่เนื้อหาจริง 26–146px ต้องปล่อยกล่องให้ยืดก่อนวัด:

```js
const contentH = dyn => { const p = dyn.style.cssText;
  dyn.style.cssText = 'flex:0 0 auto;height:auto;max-height:none;overflow:visible';
  const h = Math.round(dyn.getBoundingClientRect().height); dyn.style.cssText = p; return h; };

const cats = ['food','groceries','services','healthBeauty','leisureTravel','shopping','education','events','rent','sale'];
const rows = [];
cats.forEach(c => { openCategory(c);
  for (let i=0;i<currentCatItems.length;i++){ const it=currentCatItems[i]; if(!it._offer) continue;
    openCardDetail(i); const d=document.getElementById('detailDyn');
    rows.push({cta:it._offer.ctaType, box:d.clientHeight, need:contentH(d), name:it.name.slice(0,20)});
    closeDetail(); openCategory(c); } });
console.table(rows.sort((a,b)=>b.need-a.need));
```

---

## 7. นอกขอบเขตงานนี้ — 6 ใบที่ยังไงก็ไม่พอ

แก้ที่ข้อความไม่ช่วยพวกนี้เลย ต้องแก้ที่ตัวรายการเอง

| เนื้อหา | ต้องการ | ทางที่น่าจะใช่ |
|---|---|---|
| ลิสต์ห้องเช่า 39 by Sansiri | 1,372px | 2 คอลัมน์ หรือตัดเหลือ 5–6 ห้อง ที่เหลือไปต่อบนมือถือ |
| ลิสต์ห้องขาย 39 by Sansiri | 1,006px | เหมือนกัน |
| ตารางจอง Vistro × 4 ใบ | 424px | ช่องละ 15 นาที 10:30–21:00 = 42 ปุ่ม → เลื่อนแนวนอน หรือจัดกลุ่ม เช้า/บ่าย/ค่ำ |

---

## 8. เจอระหว่างวัด — ป๊อปอัปแสดง "คำอธิบาย" ไม่เหมือนกันสองเส้นทาง

- แตะ CTA **บนสไลด์** → `sub` = คำอธิบายของ offer (`description_th`) ยาวได้ถึง 4 บรรทัด
- แตะการ์ด **ในหน้าหมวดหมู่** (`openCardDetail`) → `sub` = **ชื่อร้าน** ไม่ใช่คำอธิบาย

เป็นคนละข้อมูลกันทั้งที่เป็นป๊อปอัปตัวเดียวกัน · ไม่ได้อยู่ในงานนี้ แต่ควรตัดสินใจสักทางว่าจะให้เห็นอะไร
เพราะ clamp 1 บรรทัดจะทำให้ความต่างนี้เห็นชัดขึ้น (ทางหนึ่งได้คำอธิบายท่อนแรก อีกทางได้ชื่อร้านเฉย ๆ)

---

## 9. รีวิวจาก session ที่ถือ `vertical-signage.html` — อ่านก่อนลงมือ

ตรวจกับโค้ดจริงบน `6155143` แล้ว: **เลขบรรทัดถูกทุกจุด** (1625 · 1644 · 1645 · 1921–1926 · 3107 · 3126 · 4288 · 4379)
· เคสยกเว้นประกาศ (ข้อ 5) จริงตามโค้ด · panel 53% ถูกต้อง · การแก้ตัวเลขเป็น 16→20 และการยกเหตุผลหลัก
ไปที่ "พื้นที่หยุดผูกกับความยาวข้อความ" — เห็นด้วย นั่นคือเหตุผลเดียวกับที่ panel ถูกล็อกไว้แต่แรก

**เห็นชอบให้ทำ** แต่มี 3 เรื่องที่สเปคยังไม่ครอบคลุมและจะพังถ้าลอกจากสไลด์ตรง ๆ

### 9.1 “กางในที่” ชนกับ panel ที่ล็อกความสูง — ข้อ 4 ยังไม่พอ

`.detail-body` (บรรทัด 1608–1611) คือ `height: 53%` **คงที่** + `overflow: hidden` โดยตั้งใจ
(ข้อความเริ่มที่ 37% เท่ากันทุกใบ) — ตัวที่เลื่อนได้คือ `.detail-dyn` เท่านั้น

ฉะนั้นประโยค *“ตอนกางออก ให้ .detail-dyn หดตามปกติ ไม่ต้องทำอะไรเพิ่ม”* **ไม่จริงในเคสยาว**:
คำอธิบาย 4 บรรทัด + พาดหัว 2 บรรทัด + รายการ อาจรวมเกิน 276px แล้ว `overflow: hidden`
จะ **ตัดข้อความที่เพิ่งกางทิ้งเงียบ ๆ** — คนกด `...more` แล้วยังอ่านไม่ครบ ซึ่งแย่กว่าไม่ให้กด

ต้องเลือกทางใดทางหนึ่ง แล้วเขียนลงสเปคก่อนทำ:
- **ก)** ตอนกาง ให้ `.detail-sub` เองมี `max-height` + `overflow-y: auto` (เลื่อนเฉพาะข้อความ) — panel นิ่ง
- **ข)** ตอนกาง ปลด `overflow: hidden` ของ panel ชั่วคราวให้ทั้งแผงเลื่อนได้ — ง่ายกว่าแต่ทำให้พาดหัว/ปุ่มขยับ
- แนะนำ **ก)** เพราะรักษาหลัก “ปุ่มหลักไม่ขยับ” ที่เพิ่งแก้กันไป

### 9.2 อย่าใช้คลาส `.slot-more` ซ้ำ — จะเปิดป๊อปอัปซ้อน

ตัวจับแตะที่บรรทัด 4674 คือ `e.target.closest('.cta, .slot-more')` → **เปิด `openDetail()`**
ถ้า chip ในป๊อปอัปใช้คลาสเดิม การแตะเพื่อ “กางข้อความ” จะกลายเป็นการเปิดป๊อปอัปใหม่ทับตัวเอง
— ขัดกับกฎข้อ 4 ของสเปคเอง

→ ใช้คลาสแยก เช่น `.detail-more` + handler ของตัวเองที่ `stopPropagation()`

### 9.3 จังหวะวัดต่างจากสไลด์โดยสิ้นเชิง

`layoutSlotText()` เป็น **one-shot ตอน `buildSlots()`** เพราะ DOM ของสไลด์สร้างครั้งเดียวแล้วอยู่ยาว
แต่ป๊อปอัปคือ **DOM ก้อนเดียวที่ถูกใช้ซ้ำทุกครั้งที่เปิด** ฉะนั้นฝั่งป๊อปอัปต้อง:

1. วัด **ทุกครั้ง** ที่ `openDetail()` เซ็ตข้อความเสร็จ (หลังบรรทัด 4288–4289)
2. **ล้าง chip เก่าและสถานะ “กางอยู่” ทุกครั้งที่เปิดใบใหม่** ไม่งั้นใบถัดไปจะเปิดมาในสภาพกางค้างจากใบก่อน
3. วัดหลังข้อความถูกเซ็ตแล้วจริง (ถ้าเซ็ตผ่าน `setTxt` แบบ sync ก็วัดต่อได้เลย ไม่ต้องรอ rAF)

ยืมได้แค่ **แนวคิด + `MORE_LABEL`** ไม่ใช่ตัวฟังก์ชัน

### 9.4 เรื่องรอง (ไม่บล็อกงาน)

- **พาดหัว clamp 2 → 1 อาจได้ไม่คุ้มเสีย**: ป๊อปอัปเพิ่งลดพาดหัวเหลือ `2.03vh` (−25%) ชื่อส่วนใหญ่จึงพอดี
  1 บรรทัดอยู่แล้ว การบังคับ 1 บรรทัดคืนแค่ 22px แต่ทำให้ชื่อที่ยาวจริงถูกตัดทันที — วัดก่อนว่ากี่ใบยังยาว
  เกิน 1 บรรทัดที่ขนาดใหม่ ถ้าน้อยกว่า ~5 ใบ แนะนำ **แก้เฉพาะคำอธิบาย** พอ
- **ข้อ 8 ควรตัดสินก่อน ไม่ใช่หลัง**: ถ้าเส้นทาง “แตะการ์ดในหมวดหมู่” ใส่ *ชื่อร้าน* ลง `#detailSub`
  ข้อความนั้นจะสั้นจนไม่มี chip เลย ผู้ใช้จะเห็นป๊อปอัปหน้าตาเดียวกันแต่พฤติกรรมต่างกันสองแบบ
  — เลือกให้จบว่าจะโชว์ *คำอธิบาย* เสมอ แล้วค่อย clamp

### 9.5 เช็คลิสต์ก่อนบอกว่าเสร็จ

- [ ] ประกาศ (`ctx.isUpdate`) — ข้อความเต็มยังอยู่ครบ ไม่โดน clamp
- [ ] แตะ `...more` ในป๊อปอัป — กางในที่ ไม่เปิดหน้าใหม่ · แตะซ้ำหุบ
- [ ] เปิดใบ A (กางค้างไว้) → ปิด → เปิดใบ B — ต้องไม่กางค้างมาจากใบ A
- [ ] เคสข้อความยาวสุด — กางแล้วอ่านได้ครบจริง ไม่ถูก `overflow: hidden` ตัด
- [ ] ปุ่มหลักและตำแหน่งเริ่มข้อความ (37%) ไม่ขยับในทุกเคส

---

## 10. ตรวจ §9 กลับด้วยการวัด — 2 ใน 3 ข้อยืนยัน, 1 ข้อเหตุผลผิด

วัดบนโค้ดเดียวกัน เปิดครบ 27 โฆษณาของ 39-by-sansiri

### 10.1 (ตอบ §9.1) "กางแล้วโดน overflow ตัด" — **วันนี้ยังไม่เกิดสักใบ แต่ข้อเสนอ (ก) ควรทำ ด้วยเหตุผลอื่น**

จำลองสภาพกางเต็ม (ปลด clamp ทิ้ง) ให้ทั้ง 27 ใบพร้อมคำอธิบายจริงของ offer → **ถูกตัด 0 ใบ**
เพราะ `.detail-dyn` เป็น `flex:1 1 auto` + `min-height: 0` มันดูดการขยายของข้อความไว้หมด

| เคสหนักสุด | คำอธิบาย | ความสูง sub | `.detail-dyn` ที่เหลือ | ถูกตัด |
|---|---|---|---|---|
| นมแพะออร์แกนิค | 234 ตัวอักษร | 103px | 77px | 0px |
| แยมราสเบอรี่ฯ | 172 ตัวอักษร | 82px | 121px | 0px |

เพดานจริงอยู่ที่ **คำอธิบายยาวเกิน ~400 ตัวอักษร** (dyn ถูกบีบจนเหลือ 0) ซึ่งยังไม่มีในข้อมูลจริง

**แต่ให้ทำข้อ (ก) อยู่ดี** — เหตุผลที่ควรเขียนลงสเปคคือ *ลิสต์ใช้งานไม่ได้* ไม่ใช่ *ข้อความถูกตัด*:
ที่ 234 ตัวอักษร ลิสต์เหลือ 77px = เห็นรายการเดียวครึ่ง ฉะนั้นตอนกางควร cap `.detail-sub`
ไว้ราว **50–55% ของ panel (~140px)** แล้วให้ข้อความเลื่อนในตัวเอง — กันทั้งสองปัญหาด้วยกฎเดียว

### 10.2 (ตอบ §9.2) "ใช้ `.slot-more` ซ้ำแล้วจะเปิดป๊อปอัปทับตัวเอง" — **ข้อสรุปถูก แต่เหตุผลไม่จริง**

`document.querySelector('.detail-overlay').closest('.stage')` → **null** · ป๊อปอัปไม่ได้อยู่ใน `.stage`
ส่วน handler บรรทัด 4673 ผูกกับ `stage` ฉะนั้น chip ที่อยู่ในป๊อปอัป **ไม่มีทางวิ่งไปโดน handler นั้น**

ยังควรใช้คลาสแยกอยู่ดี แต่เพราะเหตุผลนี้แทน: `.slot-more` (บรรทัด 396–402) ตั้งขนาดเป็น `em`
ของไทป์สไลด์ (`0.62em` / `0.82em` ใน `.slot-sub`) ยืมมาใช้ในป๊อปอัปจะได้ขนาดผิดโดยไม่มีใครสังเกต

### 10.3 (ตอบ §9.3) จังหวะวัด — **ถูก** เสริมนิดเดียว

`layoutSlotText()` ไม่ได้ one-shot เป๊ะ มันถูกเรียกซ้ำที่ `document.fonts.ready` (บรรทัด 3165)
และตอน relayout เมื่อ resize (บรรทัด 3169) ด้วย — แต่ประเด็นหลักถูกต้อง: ป๊อปอัปใช้ DOM ซ้ำ
จึงต้องวัดใหม่ทุกครั้งที่เปิด และล้างสถานะกางค้าง

### 10.4 (ตอบ §9.4) พาดหัวควร clamp มั๊ย — **ควร ตามเกณฑ์ที่ §9.4 ตั้งเอง**

นับที่ขนาดใหม่ `2.03vh` แล้ว: **9 จาก 27 ใบ (33%) ยังยาว 2 บรรทัด** — มากกว่าเกณฑ์ ~5 ใบที่รีวิวตั้งไว้
→ clamp พาดหัวด้วย ได้คืน 22px ในหนึ่งในสามของโฆษณาทั้งหมด (และ `...more` รับหน้าที่ข้อความที่เกิน)
